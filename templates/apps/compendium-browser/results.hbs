<section data-document-name="{{documentName}}">
  <div class="content" data-tooltip-direction="LEFT">
    {{#if _results}}
    {{{_results}}}
    {{else}}
    <div class="empty-results">{{localize "DRAW_STEEL.COMPENDIUM.BROWSER.noResults"}}</div>
    {{/if}}
  </div>
</section>
