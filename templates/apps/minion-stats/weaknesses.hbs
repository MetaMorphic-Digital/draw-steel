<section class="tab weaknesses standard-form {{tabs.weaknesses.cssClass}}" data-group="primary" data-tab="weaknesses">
  {{#each systemFields.damage.fields.weaknesses.fields as |field|}}
  {{formGroup
  field
  value=(lookup @root.systemSource.damage.weaknesses field.name)
  rootId=@root.document.uuid
  label=(lookup @root.damageIW.labels field.name)
  classes="slim"
  }}
  {{/each}}
</section>
