<section class="tab immunities standard-form {{tabs.immunities.cssClass}}" data-group="primary" data-tab="immunities">
  {{#each systemFields.damage.fields.immunities.fields as |field|}}
  {{formGroup
  field
  value=(lookup @root.systemSource.damage.immunities field.name)
  rootId=@root.document.uuid
  label=(lookup @root.damageIW.labels field.name)
  classes="slim"
  }}
  {{/each}}
</section>
