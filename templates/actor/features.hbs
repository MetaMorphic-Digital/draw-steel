{{! Features Tab }}
<section
  class='tab features {{tab.cssClass}}'
  data-group='primary'
  data-tab='features'
>
  <ul>
    {{#each features as |feature index|}}
      <li>{{feature.name}}</li>
    {{/each}}
  </ul>
</section>
