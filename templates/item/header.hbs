<header class="sheet-header">
  <img class="profile-img" src="{{item.img}}" data-edit="img" data-action="{{ifThen isPlay "showImage" "editImage"}}" title="{{item.name}}" alt="{{item.name}}">
  <div class="header-center">
    <div class="document-name">
      {{#if isPlay}}
      <h2>{{item.name}}</h2>
      {{else}}
      {{formInput
      fields.name
      name="name"
      value=item._source.name
      placeholder=(localize "Name")
      }}
      {{/if}}
    </div>
  </div>
  <div class="header-right">
    {{> "systems/draw-steel/templates/parts/mode-toggle.hbs"}}
    <div class="item-source">
      {{#if isPlay}}
      {{system.source.label}}
      {{else}}
      <a data-action="updateSource">
        <i class="fa-solid fa-book"></i>
        {{item.system.source.label}}
      </a>
      {{/if}}
    </div>
  </div>
</header>
